import React, { useState } from "react";
import logo from "./Images/download.png";

function App() {
  const [email, setEmail] = useState("");
  const [otp, setOtp] = useState("");
  const [otpSent, setOtpSent] = useState(false);
  const [hoverBtn, setHoverBtn] = useState(null);

  const handleSendOtp = () => {
    if (!email) {
      alert("Please enter your email or username");
      return;
    }
    setOtpSent(true);
    alert(`OTP sent to ${email}`);
  };

  const handleVerify = () => {
    if (!otp) {
      alert("Please enter the OTP");
      return;
    }
    alert(`OTP verified: ${otp}`);
  };

  // Background
  const containerStyle = {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    height: "100vh",
    background: "linear-gradient(-45deg, #007b5e, #00a884, #00bcd4, #4cafef)",
    backgroundSize: "400% 400%",
    fontFamily: "Arial, sans-serif",
    margin: 0,
    animation: "gradientBG 8s ease infinite",
  };

  // Card
  const cardStyle = {
    background: "white",
    padding: "50px",
    borderRadius: "14px",
    boxShadow: "0px 6px 16px rgba(0, 0, 0, 0.15)",
    textAlign: "center",
    width: "450px", // increased
  };

  const logoStyle = {
    width: "110px", // increased
    marginBottom: "25px",
  };

  const headingStyle = {
    marginBottom: "25px",
    color: "#003d6b",
    fontSize: "24px", // increased
  };

  // ✅ One common style for ALL boxes (inputs & buttons)
  const commonBoxStyle = {
    width: "100%",
    height: "60px", // increased height
    fontSize: "18px", // increased font size
    borderRadius: "8px",
    margin: "12px 0",
    boxSizing: "border-box",
    display: "block",
    border: "1px solid #ccc", // same border thickness
    padding: "0 14px", // adjusted padding
  };

  const inputStyle = {
    ...commonBoxStyle,
    backgroundColor: "#fff",
    color: "#000",
  };

  const buttonStyle = (isHovered) => ({
    ...commonBoxStyle,
    backgroundColor: isHovered ? "#008f4a" : "#00a859",
    color: "white",
    border: "1px solid #00a859",
    cursor: "pointer",
    padding: 0,
    transition: "background-color 0.3s ease",
    fontWeight: "bold",
  });

  return (
    <div style={containerStyle}>
      {/* Gradient animation */}
      <style>
        {`
          @keyframes gradientBG {
            0% {background-position: 0% 50%;}
            50% {background-position: 100% 50%;}
            100% {background-position: 0% 50%;}
          }
        `}
      </style>

      <div style={cardStyle}>
        <img src={logo} alt="Standard Chartered Logo" style={logoStyle} />
        <h2 style={headingStyle}>Forgot Password</h2>

        {/* Email / Username */}
        <input
          type="text"
          placeholder="Enter Email / Username"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
          style={inputStyle}
        />

        {/* Send OTP */}
        <button
          onClick={handleSendOtp}
          style={buttonStyle(hoverBtn === "send")}
          onMouseEnter={() => setHoverBtn("send")}
          onMouseLeave={() => setHoverBtn(null)}
        >
          Send OTP
        </button>

        {otpSent && (
          <>
            {/* Enter OTP */}
            <input
              type="text"
              placeholder="Enter OTP"
              value={otp}
              onChange={(e) => setOtp(e.target.value)}
              style={inputStyle}
            />

            {/* Verify & Continue */}
            <button
              onClick={handleVerify}
              style={buttonStyle(hoverBtn === "verify")}
              onMouseEnter={() => setHoverBtn("verify")}
              onMouseLeave={() => setHoverBtn(null)}
            >
              Verify & Continue
            </button>
          </>
        )}
      </div>
    </div>
  );
}

export default App;
